#!/usr/bin/env -S deno run --allow-run --allow-env --allow-import --allow-read --allow-write=/tmp/fadroma
import { Watch } from './index.ts';
export default Watch.entrypoint(import.meta, Watch.test, 'index.test.ts');

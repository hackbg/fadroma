//import { DOM, Bytes, Name } from '../lib/index.ts';
//import { elById, pinSize, textVal, download, Icon } from './lib.ts';
//import { zipSync, zipStr } from '../lib/deps.ts';
//const ESImport = (mod: string, ...items: (string|false|null)[]) => {
  //items = items.filter(Boolean)
  //if (items.length > 0) {
    //return `import { ${items.join(', ')} } from "${mod}";`
  //} else {
    //return ''
  //}
//}

describe('SecretNetworkBuilder', () => {
  it('can be created from a connection', () => {})
  it('can be created from an agent', () => {})
  it('can build a contract', () => {})
  it('can upload a contract', () => {})
})

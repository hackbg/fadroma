# Fadroma Client: Secret Network

Shared part only. Implementation in `@fadroma/client-scrt-amino` (legacy)
or `@fadroma/client-scrt-grpc` (new) because they depend on different
versions of `secretjs`.

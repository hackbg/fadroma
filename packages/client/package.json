{
  "name":    "@fadroma/client",
  "version": "4.2.0",
  "type":    "module",
  "main":    "client.ts",
  "files": [ "client.ts" ],
  "scripts": {
    "ts:check":    "tsc --noEmit",
    "release:dry": "pnpm ts:check && izomorf dry",
    "release:wet": "pnpm ts:check && izomorf wet --access=public"
  }
}

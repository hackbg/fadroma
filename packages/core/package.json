{
  "name":    "@fadroma/core",
  "version": "2.0.0",
  "type":    "module",
  "main":    "core.ts",
  "files": [ "*.ts" ],
  "description": "Isomorphic base layer for implementing dAPI clients. See @fadroma/scrt for Secret Network support.",
  "scripts": {
    "check": "tsc --noEmit",
    "test":  "echo 'Test suite missing!' && true",
    "cov":   "ensuite-cov -r text -- core.spec.ts.md",
    "lcov":  "ensuite-cov -r lcov -- core.spec.ts.md",
    "ubik":  "npm run check && npm test && ubik"
  },
  "dependencies": {
    "@hackbg/allo": "^1",
    "@hackbg/cmds": "^1",
    "@hackbg/hide": "^1",
    "@hackbg/into": "^1.0.1",
    "@hackbg/logs": "^1",
    "@hackbg/many": "^1",
    "@hackbg/oops": "^1",
    "@hackbg/over": "^1.0.1",
    "@hackbg/task": "^1"
  },
  "devDependencies": {
    "@hackbg/ensuite": "^1.1"
  }
}

# Fadroma Derive Composable

Procedural macros integrating `fadroma-composability` with `fadroma-derive-contract`.

Status: prototype. Intent to fold this into `fadroma-derive-contract` once complete.


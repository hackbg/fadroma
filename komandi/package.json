{
  "name": "@hackbg/komandi",
  "version": "2.0.0",
  "main": "dist/komandi.js",
  "devDependencies": {
    "typescript":  "latest",
    "@types/node": "latest"
  },
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && tsc -p tsconfig.json"
  }
}

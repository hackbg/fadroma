{
  "name": "@hackbg/komandi",
  "version": "1.0.0",
  "main": "dist/komandi.js",
  "devDependencies": {
    "typescript":  "latest",
    "@types/node": "latest"
  },
  "peerDependencies": {
    "@ganesha/node": "workspace:*"
  },
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && tsc -p tsconfig.json",
    "postinstall": "rm -rf dist && mkdir -p dist && tsc -p tsconfig.json",
    "test": "node --experimental-loader=@ganesha/nodejs-loader README.md"
  }
}

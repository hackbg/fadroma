{
  "name": "@hackbg/komandi",
  "version": "1.0.0",
  "main": "dist/komandi.js",
  "devDependencies": {
    "typescript": "latest"
  },
  "peerDependencies": {
    "@ganesha/nodejs": "workspace:*"
  },
  "scripts": {
    "build":       "rm -rf dist && mkdir -p dist && tsc --outDir dist komandi.ts",
    "postinstall": "rm -rf dist && mkdir -p dist && tsc --outDir dist komandi.ts",
    "test":  "node --experimental-loader=@ganesha/nodejs-loader README.md"
  }
}
